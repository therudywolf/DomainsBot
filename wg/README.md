# WireGuard

Положите сюда конфиг **wg0.conf**. WireGuard запускается из собственного образа проекта (сборка из `wireguard/`). Файл в `.gitignore` и в репозиторий не попадёт.

Подробнее: [docs/WIREGUARD_SETUP_CHECKLIST.md](../docs/WIREGUARD_SETUP_CHECKLIST.md).
