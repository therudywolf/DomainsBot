# 🚀 Быстрый старт - BotTGDomains "Под ключ"

Это руководство поможет вам развернуть бота за 5 минут.

## 📋 Требования

- **Docker** 20.10+ и **Docker Compose** 2.0+
- **Telegram Bot Token** (получить у [@BotFather](https://t.me/BotFather))
- **Telegram User ID** (получить у [@userinfobot](https://t.me/userinfobot))

## ⚡ Быстрая установка (3 шага)

### Шаг 1: Клонирование и подготовка

```bash
git clone https://github.com/therudywolf/BotTGDomains.git
cd BotTGDomains
mkdir -p data
```

### Шаг 2: Настройка конфигурации

```bash
# Копируем пример конфигурации
cp tg_domain_scanner_final/.env.example tg_domain_scanner_final/.env

# Редактируем файл (укажите свои значения)
nano tg_domain_scanner_final/.env
# или
vim tg_domain_scanner_final/.env
```

**Минимальная конфигурация** (обязательно заполните):

```env
TG_TOKEN=ваш_токен_от_BotFather
ADMIN_ID=ваш_telegram_user_id
```

**Пример:**
```env
TG_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_ID=6323277521
REQUEST_ACCESS_URL=https://t.me/your_username
```

### Шаг 3: Запуск

```bash
# Запускаем все сервисы
docker compose up -d --build

# Проверяем статус
docker compose ps

# Смотрим логи
docker compose logs -f tgscanner
```

**Готово!** Бот запущен и готов к работе. Отправьте `/start` боту в Telegram.

## 🔍 Проверка работы

1. **Проверьте статус контейнеров:**
   ```bash
   docker compose ps
   ```
   Все сервисы должны быть в состоянии `Up (healthy)`

2. **Проверьте логи:**
   ```bash
   docker compose logs tgscanner | tail -20
   ```
   Должно быть: `Бот запущен и готов к работе`

3. **Отправьте боту команду `/start`** в Telegram

## 📝 Получение необходимых данных

### Получение Telegram Bot Token

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям:
   - Укажите имя бота (например: `My Domain Scanner`)
   - Укажите username бота (например: `mydomainscanner_bot`)
4. Скопируйте полученный токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Получение Telegram User ID

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте `/start`
3. Бот покажет ваш ID (например: `6323277521`)
4. Скопируйте этот ID

## 🛠️ Управление ботом

### Остановка
```bash
docker compose down
```

### Перезапуск
```bash
docker compose restart tgscanner
```

### Просмотр логов
```bash
# Все сервисы
docker compose logs -f

# Только бот
docker compose logs -f tgscanner

# Только GOST сервисы
docker compose logs -f gostsslcheck1 gostsslcheck2 gostsslcheck3
```

### Обновление
```bash
# Остановка
docker compose down

# Обновление кода
git pull

# Пересборка и запуск
docker compose up -d --build
```

## ⚙️ Дополнительные настройки

Все дополнительные настройки (таймауты, rate limiting, логирование) можно изменить в файле `tg_domain_scanner_final/.env`. Все параметры имеют значения по умолчанию и не требуют обязательной настройки.

### Примеры настроек:

```env
# Увеличить таймауты для медленных сетей
DNS_TIMEOUT=10
HTTP_TIMEOUT=15
GOST_CHECK_TIMEOUT=30

# Увеличить rate limiting
RATE_LIMIT_REQUESTS=60
RATE_LIMIT_WINDOW=60

# Включить детальное логирование
LOG_LEVEL=DEBUG
```

## ❓ Решение проблем

### Бот не запускается

1. Проверьте логи: `docker compose logs tgscanner`
2. Убедитесь, что `TG_TOKEN` и `ADMIN_ID` заполнены в `.env`
3. Проверьте формат токена (должен быть: `число:строка`)

### GOST проверка не работает

1. Проверьте статус GOST сервисов: `docker compose ps`
2. Проверьте логи: `docker compose logs gostsslcheck1`
3. Убедитесь, что все 3 контейнера в состоянии `healthy`

### Ошибка "ADMIN_ID не задан"

Убедитесь, что в `tg_domain_scanner_final/.env` указан `ADMIN_ID=ваш_id`

## 📚 Дополнительная документация

- **[README.md](README.md)** - Общее описание проекта
- **[docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)** - Подробное руководство по развертыванию
- **[docs/API.md](docs/API.md)** - Документация по API и командам
- **[DEPLOYMENT_OFFLINE.md](DEPLOYMENT_OFFLINE.md)** - Развертывание без интернета

## 🎉 Готово!

Бот полностью настроен и готов к работе. Все настройки имеют значения по умолчанию, требуется только указать `TG_TOKEN` и `ADMIN_ID`.
