# 🔎 BotTGDomains — Telegram-бот для анализа доменов

![Python](https://img.shields.io/badge/python-3.12-blue)
![License](https://img.shields.io/badge/license-MIT-green)
![Docker](https://img.shields.io/badge/docker-ready-blue)

> Профессиональный Telegram-бот для комплексного анализа доменов с поддержкой GOST-шифрования, мониторинга и гибкой системой разрешений.

## 📚 Документация

Полная документация доступна в директории `docs/`:

- **[API.md](docs/API.md)** - Документация по API, командам и использованию
- **[ARCHITECTURE.md](docs/ARCHITECTURE.md)** - Архитектура системы и компоненты
- **[DEPLOYMENT.md](docs/DEPLOYMENT.md)** - Руководство по развертыванию

## ✨ Основные возможности

### 🔍 Проверка доменов
- **DNS записи**: A, AAAA, MX, NS записи
- **SSL сертификаты**: 
  - Обычный сертификат (срок действия, издатель, алгоритм)
  - GOST сертификат (срок действия, статус)
  - Автоматическое определение дней до истечения
- **WAF защита**: 
  - Policy-based проверка (через специальный endpoint)
  - Light проверка (анализ заголовков и статусов)
  - Переключаемый режим проверки
- **Нормализация доменов**: Автоматическая обработка URL, путей, параметров

### 📊 Мониторинг доменов
- Добавление доменов в мониторинг
- Настраиваемый интервал проверки (от 1 минуты до 24 часов)
- Уведомления об изменениях:
  - Статус GOST сертификата
  - Статус WAF
  - Сроки действия сертификатов
  - DNS записи
- Включение/выключение мониторинга

### 📋 История проверок
- Сохранение истории всех проверок
- Просмотр последних проверок
- Фильтрация по доменам

### ⚙️ Настройки
- **Режим отчета**:
  - 🔎 Расширенный (полная информация: DNS, SSL, WAF)
  - 📄 Короткий (только основные данные: даты, WAF, GOST)
- **Режим WAF проверки**:
  - Policy-based (детальная проверка)
  - Light check (быстрая проверка)
- **Таймаут WAF проверки**: Настраиваемый для каждого пользователя

### 💬 Inline режим
- Быстрая проверка доменов из любого чата
- Использование: `@YourBotName example.com`
- Автоматическая нормализация

### 📄 Загрузка файлов
- Поддержка TXT файлов со списком доменов
- Автоматическая обработка множественных доменов
- CSV экспорт при 4+ доменах

### 👨‍💼 Админ-панель
- **Управление доступом**:
  - Добавление/удаление пользователей
  - Список всех пользователей с разрешениями
- **Система разрешений**:
  - 🔍 Проверка доменов
  - 📊 Мониторинг доменов
  - 📋 История проверок
  - ⚙️ Настройки
  - 💬 Inline режим
  - 📄 Загрузка файлов
- **Статистика**: Просмотр статистики использования бота

## ⚡️ Быстрый старт

### Требования
- Docker и Docker Compose
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/therudywolf/BotTGDomains.git
cd BotTGDomains
```

2. **Создайте файл `.env`** (для Docker используется `tg_domain_scanner_final/.env`):
```bash
cp tg_domain_scanner_final/.env.example tg_domain_scanner_final/.env
```

3. **Настройте переменные окружения:**
```env
TG_TOKEN=your_telegram_bot_token
ADMIN_ID=your_telegram_user_id
REQUEST_ACCESS_URL=https://t.me/your_username
```

4. **Запустите бота:**
```bash
docker compose up --build -d
```

5. **Проверьте статус:**
```bash
docker compose ps
docker compose logs -f tgscanner
```

## 🏗️ Архитектура

```
BotTGDomains/
├── tg_domain_scanner_final/      # Основной код бота
│   ├── bot.py                    # Главный файл бота
│   ├── config.py                 # Конфигурация
│   ├── requirements.txt          # Python зависимости
│   ├── Dockerfile                # Docker образ бота
│   ├── docker-compose.yml        # Docker Compose для бота
│   └── utils/                     # Утилиты
│       ├── cache.py               # Кэширование (TTL настраивается)
│       ├── dns_utils.py           # DNS проверки
│       ├── ssl_utils.py           # SSL/GOST проверки
│       ├── waf_utils.py           # WAF проверки
│       ├── waf_light_check.py    # Light WAF проверка
│       ├── domain_normalizer.py  # Нормализация доменов
│       ├── formatting.py          # Форматирование отчетов
│       ├── monitoring.py         # Мониторинг доменов
│       ├── prefs.py              # Настройки пользователей
│       ├── rate_limiter.py       # Rate limiting
│       ├── stats.py              # Статистика
│       ├── history.py            # История проверок
│       ├── logger_config.py      # Логирование
│       └── telegram_utils.py     # Утилиты Telegram
├── GostSSLCheck/                 # Сервис проверки GOST
│   ├── server.py                 # HTTP сервер
│   ├── check.sh                  # Скрипт проверки
│   └── Dockerfile                # Docker образ
├── docker-compose.yml            # Главный Docker Compose
├── README.md                     # Этот файл
└── CHANGELOG.md                  # История изменений
```

## 🔹 Особенности

### Производительность
- **Асинхронность**: Весь I/O на `asyncio`, автоматическое использование `uvloop`
- **Кэширование**: Результаты проверок кэшируются (по умолчанию 1 час, настраивается через CACHE_TTL_SECONDS)
- **Rate Limiting**: Защита от злоупотреблений (по умолчанию 30 запросов в минуту)
- **Параллельная обработка**: Одновременная проверка нескольких доменов

### Надежность
- **Retry механизм**: Автоматические повторы при сбоях
- **Graceful shutdown**: Корректное завершение работы
- **Health checks**: Проверка здоровья сервисов
- **Логирование**: Детальные логи с ротацией

### Безопасность
- **Система разрешений**: Гибкое управление доступом к функциям
- **Валидация входных данных**: Проверка и нормализация доменов
- **Rate limiting**: Защита от DDoS и злоупотреблений

### Удобство
- **Красивые отчеты**: Структурированные отчеты с эмодзи
- **Интерактивные кнопки**: Быстрые действия прямо в отчете
- **Inline режим**: Проверка из любого чата
- **Главное меню**: Быстрый доступ ко всем функциям

## 📖 Использование

### Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Запуск бота, показ главного меню |
| `/help` | Подробная справка по использованию |
| `/monitor` | Управление мониторингом доменов |
| `/history` | Просмотр истории проверок |
| `/stats` | Статистика использования (только админ) |

### Проверка доменов

**Простая проверка:**
```
example.com
```

**Множественная проверка:**
```
example.com
google.com
github.com
```

**Из URL:**
```
https://example.com/path?param=value
→ автоматически преобразуется в example.com
```

**Через файл:**
Отправьте `.txt` файл со списком доменов (по одному на строку)

### Мониторинг доменов

1. Используйте команду `/monitor` или кнопку "📊 Мониторинг"
2. Добавьте домены для мониторинга
3. Установите интервал проверки (в минутах)
4. Включите мониторинг
5. Получайте уведомления об изменениях

### Настройки

**Режим отчета:**
- 🔎 **Расширенный**: Полная информация (DNS, SSL детали, WAF)
- 📄 **Короткий**: Только основные данные (даты сертификатов, WAF, GOST)

**Режим WAF:**
- **Policy-based**: Детальная проверка через специальный endpoint
- **Light check**: Быстрая проверка по заголовкам и статусам

### Inline режим

В любом чате Telegram:
```
@YourBotName example.com
```

Бот автоматически проверит домен и отправит результат.

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательно | По умолчанию |
|-----------|----------|-------------|--------------|
| `TG_TOKEN` | Токен Telegram бота | ✅ Да | — |
| `ADMIN_ID` | Telegram ID администратора | ✅ Да | — |
| `REQUEST_ACCESS_URL` | Ссылка для запроса доступа | ❌ Нет | — |
| `DNS_TIMEOUT` | Таймаут DNS запросов (сек) | ❌ Нет | 5 |
| `MAX_DOMAINS_PER_REQUEST` | Максимум доменов за запрос | ❌ Нет | 50 |
| `GOST_CHECK_TIMEOUT` | Таймаут проверки GOST (сек) | ❌ Нет | 15 |
| `RATE_LIMIT_WINDOW` | Окно rate limiting (сек) | ❌ Нет | 60 |
| `RATE_LIMIT_REQUESTS` | Максимум запросов в окне | ❌ Нет | 30 |
| `CACHE_TTL_SECONDS` | Время жизни кэша (сек) | ❌ Нет | 3600 |
| `LOG_LEVEL` | Уровень логирования | ❌ Нет | INFO |
| `LOG_FILE` | Путь к файлу логов | ❌ Нет | data/bot.log |

### Файлы данных

Все данные сохраняются в директории `data/`:
- `access_db.json` - База данных доступов и разрешений
- `domain_cache.db` - Кэш проверок доменов
- `user_prefs.db` - Настройки пользователей
- `monitoring_db.json` - Данные мониторинга
- `stats.json` - Статистика использования
- `check_history.json` - История проверок

## 🛠️ Разработка

### Локальная разработка

```bash
# Установка зависимостей
cd tg_domain_scanner_final
pip install -r requirements.txt

# Запуск бота
python bot.py
```

### Тестирование

```bash
# Запуск тестов
pytest tests/
```

### Логирование

Логи сохраняются в:
- Консоль (stdout)
- Файл `data/bot.log` (с ротацией, макс. 10MB, 5 копий)

## 📊 Статистика

Бот собирает статистику:
- Общее количество проверок
- Успешные/неуспешные проверки
- Популярные домены
- Активные пользователи
- Использование команд
- Ошибки

Доступна через команду `/stats` (только для админа).

## 🔐 Система разрешений

Администратор может управлять доступом пользователей к функциям:

1. **Добавить пользователя**: Админ-панель → Добавить доступ
2. **Настроить разрешения**: Админ-панель → Управление разрешениями
3. **Выбрать пользователя** и переключить нужные разрешения

### Разрешения по умолчанию

При добавлении нового пользователя:
- ✅ Проверка доменов
- ✅ Настройки
- ✅ Inline режим
- ❌ Мониторинг
- ❌ История
- ❌ Загрузка файлов

## 🐳 Docker

### Структура сервисов

- **tgscanner**: Основной бот
- **gostsslcheck**: Сервис проверки GOST (2 реплики для отказоустойчивости)

### Команды

```bash
# Запуск
docker compose up -d

# Остановка
docker compose down

# Просмотр логов
docker compose logs -f tgscanner
docker compose logs -f gostsslcheck

# Перезапуск
docker compose restart tgscanner

# Обновление
docker compose pull
docker compose up -d --build
```

## 📝 Примеры использования

### Пример 1: Простая проверка
```
Пользователь: example.com
Бот: [Отчет с DNS, SSL, WAF, GOST]
```

### Пример 2: Мониторинг
```
Пользователь: /monitor
Бот: [Меню мониторинга]
Пользователь: [Добавить домен] → example.com
Пользователь: [Установить интервал] → 30
Пользователь: [Включить мониторинг]
Бот: [Уведомления при изменениях]
```

### Пример 3: Inline режим
```
В любом чате:
@YourBotName github.com
→ Бот отправляет отчет в чат
```

## 🐛 Решение проблем

### Бот не отвечает
1. Проверьте логи: `docker compose logs tgscanner`
2. Убедитесь, что токен правильный
3. Проверьте доступ к интернету

### GOST проверка не работает
1. Проверьте логи: `docker compose logs gostsslcheck`
2. Убедитесь, что сервис запущен: `docker compose ps`
3. Проверьте health check: `docker compose exec gostsslcheck curl localhost:8080/health`

### Ошибки доступа
1. Убедитесь, что пользователь добавлен в базу доступа
2. Проверьте разрешения в админ-панели
3. Убедитесь, что ADMIN_ID правильный

## 📄 Лицензия

MIT License - см. файл LICENSE

## 👤 Автор

[therudywolf](https://github.com/therudywolf)

По всем вопросам или идеям: issue, pull request, или личка в Telegram.

---

> Вой-вой! 🪶
