# Руководство по развертыванию BotTGDomains

## Требования к системе

### Минимальные требования
- **ОС:** Linux (Ubuntu 20.04+, Debian 11+, или аналогичная)
- **Docker:** 20.10+
- **Docker Compose:** 2.0+
- **RAM:** 2 GB минимум, 4 GB рекомендуется
- **Диск:** 10 GB свободного места
- **CPU:** 2 ядра минимум

### Рекомендуемые требования
- **RAM:** 8 GB
- **Диск:** 50 GB (SSD рекомендуется)
- **CPU:** 4+ ядра
- **Сеть:** Стабильное интернет-соединение

---

## Подготовка

### 1. Клонирование репозитория

```bash
git clone <repository_url>
cd BotTGDomains
```

### 2. Создание файла `.env`

Создайте файл `.env` в корне проекта:

```env
# Обязательные параметры
TG_TOKEN=your_telegram_bot_token_here
ADMIN_ID=your_telegram_user_id

# Опциональные параметры (значения по умолчанию)
DNS_TIMEOUT=5
HTTP_TIMEOUT=6
CONCURRENCY=20
GOST_CHECK_TIMEOUT=15
GOST_RETRY_ATTEMPTS=3
GOST_RETRY_DELAY=0.5
RATE_LIMIT_REQUESTS=30
RATE_LIMIT_WINDOW=60
LOG_LEVEL=INFO
LOG_FILE=data/bot.log
LOG_MAX_BYTES=10485760
LOG_BACKUP_COUNT=5
HISTORY_ENABLED=true
HISTORY_MAX_ENTRIES=10000
HISTORY_CLEANUP_DAYS=30
STATS_ENABLED=true
MAX_DOMAINS_PER_REQUEST=1000
MAX_FILE_SIZE_MB=10
```

### 3. Получение Telegram Bot Token

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env`

### 4. Получение Telegram User ID

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Скопируйте ваш ID
3. Вставьте в `.env` как `ADMIN_ID`

---

## Развертывание

### Вариант 1: Docker Compose (Рекомендуется)

#### Шаг 1: Создание директорий

```bash
mkdir -p data logs
```

#### Шаг 2: Запуск сервисов

```bash
docker-compose up -d
```

#### Шаг 3: Проверка статуса

```bash
docker-compose ps
```

Все сервисы должны быть в состоянии `Up (healthy)`.

#### Шаг 4: Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только бот
docker-compose logs -f tgscanner

# Только Gost сервисы
docker-compose logs -f gostsslcheck
```

---

### Вариант 2: Ручная установка (для разработки)

#### Шаг 1: Установка зависимостей

```bash
cd tg_domain_scanner_final
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

pip install -r requirements.txt
```

#### Шаг 2: Создание директорий

```bash
mkdir -p data logs
```

#### Шаг 3: Настройка переменных окружения

Создайте `.env` файл (см. выше).

#### Шаг 4: Запуск бота

```bash
python bot.py
```

**Примечание:** Для проверки GOST сертификатов необходимо также запустить сервисы `GostSSLCheck` через Docker Compose.

---

## Настройка

### Доступ пользователей

По умолчанию доступ имеет только администратор. Для добавления пользователей:

1. Запустите бота
2. Отправьте `/start` от имени администратора
3. Нажмите "👨‍💼 Админ-панель"
4. Выберите "➕ Добавить доступ"
5. Введите Telegram ID пользователя

### Настройка разрешений

Для каждого пользователя можно настроить разрешения:

1. Админ-панель → "⚙️ Управление разрешениями"
2. Выберите пользователя
3. Переключайте разрешения кнопками

**Разрешения:**
- `check_domains` - проверка доменов
- `monitoring` - мониторинг доменов
- `history` - просмотр истории
- `settings` - изменение настроек
- `file_upload` - загрузка файлов

---

## Мониторинг и обслуживание

### Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Последние 100 строк
docker-compose logs --tail=100

# Логи за последний час
docker-compose logs --since 1h
```

### Проверка состояния

Используйте команду `/health` в боте (только для администратора).

### Очистка данных

#### Очистка истории
История автоматически очищается от записей старше 30 дней (настраивается через `HISTORY_CLEANUP_DAYS`).

#### Очистка кэша
Кэш автоматически ограничивается по размеру (20,000 записей по умолчанию).

#### Очистка логов
Логи автоматически ротируются (10 MB, 5 копий).

### Резервное копирование

Рекомендуется регулярно создавать резервные копии директории `data/`:

```bash
# Создание резервной копии
tar -czf backup_$(date +%Y%m%d_%H%M%S).tar.gz data/

# Восстановление
tar -xzf backup_YYYYMMDD_HHMMSS.tar.gz
```

---

## Обновление

### Обновление через Docker Compose

```bash
# Остановка сервисов
docker-compose down

# Обновление кода
git pull

# Пересборка образов
docker-compose build

# Запуск
docker-compose up -d
```

### Обновление зависимостей

```bash
# Обновление requirements.txt
pip install --upgrade -r requirements.txt

# Пересборка Docker образа
docker-compose build --no-cache tgscanner
```

---

## Troubleshooting

### Проблема: Бот не отвечает

**Проверьте:**
1. Статус контейнеров: `docker-compose ps`
2. Логи: `docker-compose logs tgscanner`
3. Правильность токена в `.env`
4. Доступность интернета

**Решение:**
```bash
# Перезапуск бота
docker-compose restart tgscanner

# Просмотр логов
docker-compose logs -f tgscanner
```

---

### Проблема: Ошибки проверки GOST

**Проверьте:**
1. Статус Gost сервисов: `docker-compose ps gostsslcheck`
2. Логи: `docker-compose logs gostsslcheck`
3. Health check: `curl http://localhost:8080/health` (для первого сервиса)

**Решение:**
```bash
# Перезапуск Gost сервисов
docker-compose restart gostsslcheck

# Пересборка образов
docker-compose build gostsslcheck
docker-compose up -d gostsslcheck
```

---

### Проблема: Превышение лимитов памяти

**Симптомы:**
- Медленная работа бота
- Ошибки "Out of memory"

**Решение:**
1. Уменьшите `CONCURRENCY` в `.env`
2. Уменьшите `HISTORY_MAX_ENTRIES`
3. Увеличьте RAM сервера
4. Настройте более частую очистку истории

---

### Проблема: Ошибки записи в файлы

**Проверьте:**
1. Права доступа к директории `data/`
2. Свободное место на диске
3. Логи на наличие ошибок I/O

**Решение:**
```bash
# Проверка прав
ls -la data/

# Установка правильных прав
chmod -R 755 data/
chown -R $USER:$USER data/

# Проверка свободного места
df -h
```

---

### Проблема: Rate limiting слишком строгий

**Решение:**
Увеличьте лимиты в `.env`:
```env
RATE_LIMIT_REQUESTS=50
RATE_LIMIT_WINDOW=60
```

Или для тяжелых операций измените в коде `rate_limiter.py`.

---

## Безопасность

### Рекомендации

1. **Храните `.env` в безопасности**
   - Не коммитьте `.env` в git
   - Используйте `.gitignore`
   - Ограничьте доступ к файлу: `chmod 600 .env`

2. **Ограничьте доступ к серверу**
   - Используйте firewall
   - Настройте SSH ключи вместо паролей
   - Регулярно обновляйте систему

3. **Мониторинг**
   - Регулярно проверяйте логи на подозрительную активность
   - Настройте алерты для критических ошибок
   - Мониторьте использование ресурсов

4. **Резервное копирование**
   - Регулярно создавайте резервные копии `data/`
   - Храните копии в безопасном месте
   - Тестируйте восстановление из резервных копий

---

## Производительность

### Оптимизация для высокой нагрузки

1. **Увеличьте concurrency:**
   ```env
   CONCURRENCY=50
   ```

2. **Настройте rate limiting:**
   ```env
   RATE_LIMIT_REQUESTS=100
   RATE_LIMIT_WINDOW=60
   ```

3. **Используйте SSD** для директории `data/`

4. **Увеличьте ресурсы Docker:**
   - RAM: 8+ GB
   - CPU: 4+ ядер

---

## Поддержка

При возникновении проблем:

1. Проверьте логи: `docker-compose logs`
2. Используйте команду `/health` в боте
3. Проверьте документацию в `docs/`
4. Создайте issue в репозитории с:
   - Описанием проблемы
   - Логами (без чувствительных данных)
   - Шагами для воспроизведения
   - Версией Docker и ОС

---

## Дополнительные ресурсы

- [Документация aiogram](https://docs.aiogram.dev/)
- [Документация Docker Compose](https://docs.docker.com/compose/)
- [Telegram Bot API](https://core.telegram.org/bots/api)





