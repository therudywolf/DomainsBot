# API Документация BotTGDomains

## Обзор

BotTGDomains - это Telegram бот для проверки доменов, мониторинга изменений и анализа SSL/TLS сертификатов, включая поддержку ГОСТ сертификатов.

## Команды

### Основные команды

#### `/start`
Запускает бота и показывает главное меню.

**Доступ:** Все пользователи с доступом

**Ответ:** Приветственное сообщение с главным меню

---

#### `/help`
Показывает справку по использованию бота.

**Доступ:** Все пользователи с доступом

**Ответ:** Подробная справка с описанием всех функций

---

#### `/history`
Показывает историю проверок пользователя.

**Доступ:** Пользователи с разрешением `history`

**Ответ:** Список последних проверенных доменов (до 10 записей)

**Пример:**
```
/history
```

---

#### `/export_history`
Экспортирует историю проверок в CSV файл.

**Доступ:** Пользователи с разрешением `history`

**Ответ:** CSV файл с полной информацией о проверках

**Формат CSV:**
- Дата и время
- Домен
- GOST статус
- WAF статус
- Метод проверки WAF
- Даты сертификатов (обычный и GOST)
- DNS записи (A, AAAA, MX, NS)

---

#### `/monitor`
Управление мониторингом доменов.

**Доступ:** Пользователи с разрешением `monitoring`

**Ответ:** Меню управления мониторингом с кнопками:
- ➕ Добавить домен
- ➖ Удалить домен
- 📋 Список доменов
- 📥 Экспорт
- ⏱️ Интервал
- ⚙️ WAF таймаут
- 🔄 Вкл/Выкл

**Подкоманды:**
- Добавление доменов: можно вводить несколько через пробел, запятую или с новой строки
- Также поддерживается загрузка TXT файла со списком доменов

---

#### `/compare domain1.com domain2.com`
Сравнивает два домена.

**Доступ:** Пользователи с разрешением `check_domains`

**Параметры:**
- `domain1.com` - первый домен для сравнения
- `domain2.com` - второй домен для сравнения

**Ответ:** Детальное сравнение:
- DNS записи (A, AAAA, MX, NS)
- SSL сертификаты (GOST, даты)
- WAF статус

**Пример:**
```
/compare example.com test.com
```

---

### Административные команды

#### `/stats`
Показывает статистику использования бота.

**Доступ:** Только администратор

**Ответ:** Статистика включает:
- Время работы бота
- Количество проверенных доменов
- Количество пользователей
- Топ доменов
- Топ команд
- Топ ошибок
- Активность по часам
- Метрики производительности
- Статистика кэша

**Экспорт:** Кнопки для экспорта в JSON и CSV

---

#### `/health`
Проверка состояния системы и всех компонентов.

**Доступ:** Только администратор

**Ответ:** Статус всех компонентов:
- Кэш
- Статистика
- Мониторинг
- Rate Limiter
- Gost сервисы

---

## Проверка доменов

### Текстовый ввод
Отправьте домен(ы) текстом в бот. Поддерживаются форматы:
- `example.com`
- `https://example.com/path`
- `http://example.com?param=value`

Можно указать несколько доменов через пробел, запятую или с новой строки.

**При 4+ доменах:** Автоматически формируется CSV файл с результатами.

### Загрузка файла
Отправьте TXT файл со списком доменов (по одному на строку).

**Ограничения:**
- Максимальный размер: 10 MB (настраивается)
- Формат: только .txt
- Кодировка: UTF-8

---

## Inline режим

Используйте бота в inline режиме в любом чате:
```
@YourBotName example.com
```

**Доступ:** Все пользователи с доступом

**Ответ:** Быстрая проверка домена с кратким отчетом

---

## Интерактивные кнопки в отчетах

После проверки домена доступны следующие действия:

### Переключение режима отчета
- **🔎 Расширенный** - полный отчет с DNS записями
- **📄 Короткий** - только основные данные (даты, WAF, GOST)

### Быстрые проверки
- **🛡️ Проверить WAF** - быстрая проверка WAF через инъекции
- **📅 Проверить сертификаты** - проверка сроков действия сертификатов

### Детальный просмотр
- **📡 Детали DNS** - полная информация о DNS записях
- **🔒 Детали SSL** - детальная информация о сертификатах
- **🛡️ Детали WAF** - результаты проверки WAF разными методами

### Другие действия
- **🔄 Перепроверить домен** - повторная полная проверка
- **📤 Поделиться** - поделиться через inline режим

---

## Мониторинг доменов

### Добавление доменов
1. Нажмите "➕ Добавить домен"
2. Введите домен(ы) или отправьте TXT файл
3. Домены автоматически нормализуются

### Настройка интервала
1. Нажмите "⏱️ Интервал"
2. Введите интервал в минутах (от 1 до 1440)

### Уведомления
Бот отправляет уведомления при изменениях:
- GOST статус (Да/Нет)
- WAF статус (Да/Нет)
- Даты сертификатов (обычный и GOST)
- DNS записи (A, AAAA, MX, NS)
- Приближающееся истечение сертификатов (за 30, 14, 7 дней)

---

## Настройки

### Режим проверки WAF
- **Policy** - проверка через `/?monitoring=test_query_for_policy`
- **Light** - легкая проверка через простой GET запрос

### Таймаут WAF
Настраиваемый таймаут для проверки WAF (привязывается к Telegram ID).

---

## Форматы данных

### Отчет о домене

#### Расширенный режим
```
🌐 domain.com

📡 DNS записи
   IP: 1.2.3.4
   A: 1.2.3.4
   AAAA: —
   MX: mail.example.com
   NS: ns1.example.com, ns2.example.com

🔒 SSL сертификат (обычный)
   Домен: *.example.com
   Домены в сертификате: *.example.com, example.com
   Издатель: Let's Encrypt
   Алгоритм подписи: sha256WithRSAEncryption
   Шифр: TLS_AES_256_GCM_SHA384

📅 Сроки действия сертификатов
   Обычный сертификат: 01.01.2024 → 01.01.2025 (осталось 365 дн.)
   GOST сертификат: 01.01.2024 → 01.01.2025 (осталось 365 дн.) ✅

🛡️ Безопасность
   WAF: ✅ Включен (проверено: check policy)
   GOST сертификат: ✅ Обнаружен
```

#### Короткий режим
```
🌐 domain.com

📅 Сроки действия сертификатов
   Обычный сертификат: 01.01.2024 → 01.01.2025 (осталось 365 дн.)
   GOST сертификат: 01.01.2024 → 01.01.2025 (осталось 365 дн.) ✅

🛡️ Безопасность
   WAF: ✅ Включен (проверено: check policy)
   GOST сертификат: ✅ Обнаружен
```

### CSV формат (для 4+ доменов)

**Заголовки:**
- Domain
- A
- AAAA
- MX
- NS
- CN
- Valid From
- Valid To
- GOST Cert From
- GOST Cert To
- WAF
- GOST

---

## Разрешения

Бот поддерживает гранулярный контроль доступа через разрешения:

- `check_domains` - проверка доменов
- `monitoring` - мониторинг доменов
- `history` - просмотр истории
- `settings` - изменение настроек
- `file_upload` - загрузка файлов

Администратор имеет доступ ко всем функциям.

---

## Rate Limiting

Бот использует систему rate limiting для защиты от злоупотреблений:

- **Обычные операции:** 30 запросов в минуту
- **Тяжелые операции (проверка доменов):** 10 запросов в минуту
- **Загрузка файлов:** 5 запросов в 5 минут

При превышении лимита пользователь блокируется на 5 минут.

---

## Обработка ошибок

Все ошибки логируются с уникальным ID для отслеживания. Пользователю показывается понятное сообщение с кодом ошибки.

**Типы ошибок:**
- `DNS_ERROR` - ошибка получения DNS информации
- `SSL_ERROR` - ошибка проверки SSL сертификата
- `WAF_ERROR` - ошибка проверки WAF
- `GOST_ERROR` - ошибка проверки GOST сертификата
- `TIMEOUT_ERROR` - превышено время ожидания
- `CONNECTION_ERROR` - ошибка соединения
- `FILE_PROCESSING_ERROR` - ошибка обработки файла
- `PROCESSING_ERROR` - общая ошибка обработки

---

## Примеры использования

### Проверка одного домена
```
example.com
```

### Проверка нескольких доменов
```
example.com test.ru https://site.com/path
```

### Сравнение доменов
```
/compare example.com test.com
```

### Добавление доменов в мониторинг
1. `/monitor`
2. Нажать "➕ Добавить домен"
3. Ввести: `example.com test.ru site.com`

### Экспорт истории
```
/export_history
```

---

## Ограничения

- Максимальное количество доменов в одном запросе: 1000
- Максимальный размер файла: 10 MB
- Максимальная длина домена: 253 символа
- Максимальный размер текстового сообщения: 100 KB

---

## Поддержка

При возникновении проблем:
1. Проверьте код ошибки в сообщении
2. Обратитесь к администратору с указанием кода ошибки
3. Проверьте логи бота (для администратора)

