# WireGuard Setup Checklist для Docker контейнеров

## Архитектура

WireGuard теперь работает в **отдельном контейнере** (`masipcat/wireguard-go`), а не внутри контейнера `tgscanner`. Это решает проблемы с правами доступа и зависимостями.

### Новая архитектура:
```
wireguard (отдельный контейнер) → поднимает WG интерфейс
tgscanner → использует сеть WireGuard контейнера через Docker network
```

## ✅ Что настроено в docker-compose.yml

### WireGuard сервис:
- ✅ **image: masipcat/wireguard-go:latest** - готовый WireGuard контейнер
- ✅ **NET_ADMIN** capability - необходимо для создания сетевых интерфейсов
- ✅ **/dev/net/tun** device - TUN/TAP устройство для VPN интерфейсов
- ✅ **net.ipv4.ip_forward=1** sysctl - разрешает форвардинг пакетов
- ✅ **Volume: ./wg:/etc/wireguard:ro** - монтирование конфига WireGuard
- ✅ **Healthcheck** - проверка доступности через `wg show TGBOT`

### Docker Networks:
- ✅ **default** network - для связи между tgscanner и gostsslcheck контейнерами
- ✅ **wg_network** network - для связи между tgscanner и WireGuard контейнером

### tgscanner контейнер:
- ✅ Подключен к обеим сетям (default и wg_network)
- ✅ **Убраны** NET_ADMIN, /dev/net/tun, sysctls - больше не нужны
- ✅ **Убраны** wireguard-tools из Dockerfile - WireGuard работает в отдельном контейнере

## ✅ Что НЕ нужно в Dockerfile tgscanner

### Удаленные пакеты:
- ❌ **wireguard-tools** - больше не нужен
- ❌ **iproute2** - больше не нужен
- ❌ **openresolv** - больше не нужен

### Удаленные настройки:
- ❌ Копирование конфига WireGuard в образ
- ❌ Установка прав доступа для конфига

## ✅ Что используется в коде (wireguard_utils.py)

### Проверка доступности:
- ✅ Проверка доступности WireGuard контейнера через Docker сеть (DNS resolution)
- ✅ Использование переменной окружения `WG_CONTAINER_NAME` (по умолчанию "wireguard")

### Упрощенная логика:
- ✅ `is_wg_interface_up()` - проверяет доступность контейнера через DNS
- ✅ `ensure_wg_interface_up()` - проверяет доступность контейнера (не поднимает интерфейс)
- ✅ `ensure_wg_interface_down()` - заглушка (контейнер управляется через docker-compose)

## ⚠️ Требования к хосту для Docker развертывания

**ВАЖНО:** Проект работает ТОЛЬКО в Docker контейнерах.

### Модуль ядра WireGuard на хосте:
- **Должен быть в ядре Linux хоста**, где запускается Docker контейнер
- WireGuard контейнер использует модуль ядра хоста через `/dev/net/tun`
- Проверка на хосте: `lsmod | grep wireguard` или `modprobe wireguard`
- В современных ядрах Linux (5.6+) WireGuard встроен в ядро по умолчанию
- Если модуль не загружен: `sudo modprobe wireguard` на хосте

### Конфигурация:
- Конфиг WireGuard должен находиться в `wg/TGBOT.conf` на хосте
- Конфиг монтируется в WireGuard контейнер как read-only volume
- Формат конфига - стандартный WireGuard формат

## 📋 Итоговый чеклист

### В docker-compose.yml:
- [x] WireGuard сервис добавлен (masipcat/wireguard-go)
- [x] NET_ADMIN capability для WireGuard контейнера
- [x] /dev/net/tun device для WireGuard контейнера
- [x] net.ipv4.ip_forward=1 sysctl для WireGuard контейнера
- [x] Конфиг WireGuard монтируется как volume
- [x] Docker networks настроены (default и wg_network)
- [x] tgscanner подключен к обеим сетям
- [x] Убраны NET_ADMIN, /dev/net/tun, sysctls из tgscanner

### В Dockerfile tgscanner:
- [x] wireguard-tools удален
- [x] iproute2 удален
- [x] openresolv удален
- [x] Копирование конфига WireGuard удалено

### В коде:
- [x] wireguard_utils.py упрощен - проверка доступности контейнера
- [x] ssl_utils.py обновлен - убрана local_addr привязка
- [x] Использование Docker networks для маршрутизации

## ✅ Все готово!

WireGuard теперь работает в отдельном контейнере, что упрощает управление и устраняет проблемы с правами доступа и зависимостями.
