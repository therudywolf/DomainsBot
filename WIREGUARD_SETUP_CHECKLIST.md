# WireGuard Setup Checklist для Docker контейнеров

## ✅ Что установлено в Dockerfile (tg_domain_scanner_final/Dockerfile)

### Пакеты:
- ✅ **wireguard-tools** - содержит `wg` и `wg-quick` команды
- ✅ **iproute2** - содержит `ip` команды (ip link, ip addr, ip route) - используется wg-quick
- ✅ **openresolv** - содержит `resolvconf` (опционально, DNS настраивается автоматически в Docker)

### Что НЕ нужно устанавливать в контейнере:
- ❌ Модуль ядра WireGuard - должен быть на хосте (в ядре Linux)
- ❌ kmod - не нужен, так как модуль должен быть уже загружен на хосте

## ✅ Что настроено в docker-compose.yml

### Capabilities:
- ✅ **NET_ADMIN** - необходимо для создания сетевых интерфейсов (ip link add)

### Devices:
- ✅ **/dev/net/tun** - TUN/TAP устройство для создания VPN интерфейсов

### Sysctls:
- ✅ **net.ipv4.ip_forward=1** - разрешает форвардинг пакетов через VPN

### Volumes:
- ✅ **Конфиг WireGuard встроен в образ** - копируется при сборке Docker образа
- ⚠️  Volume для wg/ опционален - можно переопределить конфиг через volume если нужно

## ✅ Что используется в коде (wireguard_utils.py)

### Команды:
- ✅ `wg version` - проверка доступности WireGuard
- ✅ `wg show <interface>` - проверка статуса интерфейса
- ✅ `wg-quick up <config>` - поднятие интерфейса (без дополнительных флагов)
- ✅ `wg-quick down <interface>` - опускание интерфейса

### Внутренние команды wg-quick (автоматически):
- `ip link add <interface> type wireguard` - создание интерфейса
- `wg setconf <interface> <config>` - применение конфигурации
- `ip addr add <ip>/<mask> dev <interface>` - назначение IP адреса
- `ip link set mtu <mtu> up dev <interface>` - установка MTU и поднятие интерфейса

## ⚠️ Требования к хосту для Docker развертывания

**ВАЖНО:** Проект работает ТОЛЬКО в Docker контейнерах. Все зависимости установлены в Dockerfile.

### Модуль ядра WireGuard на хосте:
- **Должен быть в ядре Linux хоста**, где запускается Docker контейнер
- Контейнер использует модуль ядра хоста через `/dev/net/tun`
- Проверка на хосте: `lsmod | grep wireguard` или `modprobe wireguard`
- В современных ядрах Linux (5.6+) WireGuard встроен в ядро по умолчанию
- Если модуль не загружен: `sudo modprobe wireguard` на хосте

### Права доступа:
- Конфиг WireGuard должен иметь права 600 (устанавливается автоматически в коде внутри контейнера)
- Директория `/dev/net/tun` монтируется в docker-compose.yml автоматически

## 📋 Итоговый чеклист

### В Dockerfile:
- [x] wireguard-tools установлен
- [x] iproute2 установлен
- [x] openresolv установлен (опционально)
- [x] Конфиг WireGuard копируется в образ при сборке
- [x] Права доступа 600 устанавливаются автоматически

### В docker-compose.yml:
- [x] NET_ADMIN capability добавлена
- [x] /dev/net/tun device смонтирован
- [x] net.ipv4.ip_forward=1 установлен
- [x] Конфиг WireGuard встроен в образ (копируется при сборке)

### В коде:
- [x] Конфиг копируется в образ при сборке с правами 600
- [x] Обработка read-only файловой системы (копирование во временную директорию если нужно)
- [x] Все необходимые команды доступны через установленные пакеты

## ✅ Все готово!

Все необходимые пакеты и настройки для WireGuard установлены и настроены в контейнерах.
